name: show_fps
description: A package to show FPS on screen.
version: 1.0.3
homepage: https://github.com/mantreshkhurana/show_fps

environment:
  sdk: '>=2.18.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
    
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  